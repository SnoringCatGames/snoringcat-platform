# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
*.egg-info/
.pytest_cache/
.mypy_cache/

# AWS SAM
backend/.aws-sam/
samconfig.toml
!samconfig.toml.example

# Godot
.godot/
.import/
*.import
export.cfg
export_presets.cfg

# OS
.DS_Store
Thumbs.db
desktop.ini

# IDE
.vscode/
.idea/
*.swp
*.swo

# Local dev
*.local
.env
.env.local

# Plugin build artifacts (snoringcat.so already caught by *.so above,
# but build.log + the build dir itself need to be excluded so the
# parent repo's git status stays clean when local-smoke-test.ps1 has
# been run against the dev docker-compose stack).
runtime/build/
